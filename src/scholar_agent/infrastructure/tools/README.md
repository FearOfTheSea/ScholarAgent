# Local study tools

Each tool converts structured graph inputs into a single application action or
lookup. Tools return structured data and never generate a final chat response.
The user-facing capability catalog exposes question answering, summary, quiz,
and flashcards to the constrained planner.
